Program 'bmpmirror' takes from the command line the name <filename>.bmp of a 24-bit uncompressed RGB .bmp file of any size and creates
its mirror (e.g., left and right switched) image, which it saves as <filename>mirrored.bmp . 
